# 🔧 แก้ไขปัญหา "วิเคราะห์ด้วย AI" ไม่ทำงาน

## ✅ การแก้ไขที่ทำ

### 1. เปลี่ยน ResumeAnalysisModal ให้ใช้ API Endpoint
- ✅ เปลี่ยนจากเรียก `analyzeResume` โดยตรง → เรียกผ่าน `aiAPI.analyzeResume()`
- ✅ แปลง `resume` object เป็น `resumeText` string ก่อนส่ง API
- ✅ แปลง `jobDescription` object เป็น `jobDescriptionText` string ก่อนส่ง API

### 2. แก้ไข API Route ให้รองรับทั้ง String และ Object
- ✅ แก้ไข `/api/ai/analyze-resume` ให้แปลง string เป็น object ก่อนส่งไป `analyzeResume()`
- ✅ เพิ่ม error handling ที่ดีขึ้น

### 3. แก้ไข Gemini API Key
- ✅ เปลี่ยนให้อ่าน API key จาก environment variable (`VITE_GEMINI_API_KEY` หรือ `GEMINI_API_KEY`)

---

## ⚠️ ต้องตั้งค่า API Key

### สำหรับ Frontend (Vite)
สร้างไฟล์ `.env` ใน root directory:
```env
VITE_GEMINI_API_KEY=your_gemini_api_key_here
```

### สำหรับ Backend (Server)
สร้างไฟล์ `server/.env` หรือเพิ่มใน `.env`:
```env
GEMINI_API_KEY=your_gemini_api_key_here
```

---

## 🧪 วิธีทดสอบ

1. **ตั้งค่า API Key** (ดูด้านบน)
2. **รัน API Server:**
   ```bash
   npm run dev:server
   ```
3. **รัน Frontend:**
   ```bash
   npm run dev
   ```
4. **ทดสอบ:**
   - Login เป็น HR
   - ไปที่คำขอที่มีสถานะ `screening`
   - กดปุ่ม "วิเคราะห์ด้วย AI"
   - กรอกข้อมูล Resume
   - กด "วิเคราะห์ด้วย AI"
   - ควรเห็นผลการวิเคราะห์

---

## 🐛 ถ้ายังไม่ทำงาน

### ตรวจสอบ Console
1. เปิด Browser DevTools (F12)
2. ไปที่ Console tab
3. ดู Error messages

### ตรวจสอบ Network
1. เปิด Browser DevTools (F12)
2. ไปที่ Network tab
3. ดู Request ไป `/api/ai/analyze-resume`
4. ตรวจสอบ Response

### ตรวจสอบ API Server
1. ดู Console ของ API Server
2. ตรวจสอบ Error messages

---

## ✅ Checklist

- [ ] ตั้งค่า `VITE_GEMINI_API_KEY` ใน `.env`
- [ ] ตั้งค่า `GEMINI_API_KEY` ใน `server/.env` (ถ้าใช้)
- [ ] API Server รันอยู่ (`npm run dev:server`)
- [ ] Frontend รันอยู่ (`npm run dev`)
- [ ] ทดสอบกดปุ่ม "วิเคราะห์ด้วย AI"

